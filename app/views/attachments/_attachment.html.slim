p.attachment id="attachment-#{attachment.id}"
  = link_to attachment.filename.to_s, url_for(attachment)
  - if current_user&.author_of?(attachment.record)
    = link_to 'Remove', attachment_path(attachment), remote: true, method: :delete