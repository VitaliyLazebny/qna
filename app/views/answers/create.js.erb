$('.answer_errors').html("<%= render 'shared/errors', resource: @answer %>");

<% if @answer.persisted? %>
  $('.notice').html("Your answer was successfully created.");
  $('.answers').append("<%= j render @answer %>");
  $('#answer_body').val("");
<% else %>
  $('.notice').html("");
<% end %>
